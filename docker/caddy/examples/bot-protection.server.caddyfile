# This snippet enables the post-challenge listener for TPS or Anubis to use.
#
# See docs/install/bot-protection.md for usage and bot protection details.

# This port should never be published to the host unless you're testing
# something, otherwise bots could potentially bypass your protections.
:8081 {
  import /etc/caddy/oni-routing.caddyfile

  # Everything else goes to ONI (via gunicorn), but for this internal server we
  # force the host and scheme since our bot protection proxies can lose this.
  reverse_proxy http://app:8000 {
    header_up Host {$ONI_PUBLIC_HOST}
    header_up X-Forwarded-Proto {$ONI_PUBLIC_SCHEME}
  }

  log {
    level INFO
  }
}
